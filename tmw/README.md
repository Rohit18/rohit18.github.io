# TMW

Static optical/radar flood imagery gallery containing 500 paired views.

Upload `index.html` and the `images/` directory together, preserving their relative paths.
No application server or database is required.

Before public release, confirm the final attribution and redistribution language for every source dataset.
